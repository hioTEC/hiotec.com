# Landing Backlog

Processed from the retired global runtime entry `hiotec-landing` on
2026-05-03. This file is repo-local project memory, not an active run.

## Pending

1. Footer information is incomplete. Add contact, GitHub, or other durable
   brand links.
2. Feature screenshots are placeholders. Replace with real product screenshots.
3. `ToolCard.astro` and `tools.ts` may be dead files after the landing-page
   redesign. Check before deleting.

## Rule

If this work becomes multi-session or needs parallel agents, create
`.agents/runs/active.yaml` and a run handoff in this repo. Do not put backlog or
design truth in global runtime.
